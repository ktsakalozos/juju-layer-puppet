name: puppet
summary: Installs puppet
description: |
  Installs stable Puppet packages from the archive. On Trusty, updated puppet
  packages that fix YAML parsing bugs are installed from the puppetlabs trusty
  repository.
